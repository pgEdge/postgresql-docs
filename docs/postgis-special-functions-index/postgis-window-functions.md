<a id="PostGIS_Window_Functions"></a>

## PostGIS Window Functions


The functions below are spatial window functions that are used in the same way as SQL window functions such as <code>row_number()</code>, <code>lead()</code>, and <code>lag()</code>. They must be followed by an <code>OVER()</code> clause.
